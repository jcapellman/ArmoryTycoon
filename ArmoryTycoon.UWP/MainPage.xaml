<Page
    x:Class="ArmoryTycoon.UWP.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ArmoryTycoon.UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Armory Tycoon" Style="{StaticResource HeaderTextBlockStyle}" />

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Button Grid.Row="0" Content="New Game" />

            <Button Grid.Row="1" Content="Load Game" Click="btnLoadGame_OnClick" />

            <Button Grid.Row="2" Content="Options" />
        </Grid>

        <TextBlock Grid.Row="2" Text="Version 0.0.1 - (C)2021 Motion Blur Studios, LLC" Style="{StaticResource BodyTextBlockStyle}" />
    </Grid>
</Page>